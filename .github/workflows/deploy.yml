name: Deploy Backend

on:
  push:
    branches: [ master ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.PORT }}
          script: |
            echo "${{ secrets.SSH_PRIVATE_KEY }}" | tr -d '\r' | ssh-add -
            eval $(ssh-agent -s)
            echo "${{ secrets.SSH_PRIVATE_KEY }}" | tr -d '\r' | ssh-add -
            cd /var/www/fleet-control/fleet-control-back-end
            git pull origin master
            docker exec fleet-control-app composer install --no-dev --optimize-autoloader
            docker exec fleet-control-app php artisan migrate --force
            docker exec fleet-control-app php artisan config:cache
            docker exec fleet-control-app php artisan route:cache
